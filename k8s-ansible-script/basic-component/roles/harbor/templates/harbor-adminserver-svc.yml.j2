apiVersion: v1
kind: Service
metadata:
  name: harbor-adminserver
  namespace: {{ harbor_namespace }}
  labels:
    X_APP: harbor
    app: harbor
    daemon: adminserver
    type: service
spec:
  ports:
  - name: http
    port: 8080
  selector:
    app: harbor
    daemon: adminserver
